# Brief instructions to run the benchmark
As the file contains both text sections and code sections, the user needs to run the entire notebook. To do this, click on 'Runtime' and then select 'Run all'
